const svelteMarkdown = require("./svelteMarkdown");
const resolvePages = require("./resolvePages");
const prerenderPages = require("./prerenderPages");

module.exports = {
	svelteMarkdown,
	resolvePages,
	prerenderPages,
};
